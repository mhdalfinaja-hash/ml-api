services:
  - type: web
    name: ml-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python api_ml.py"
    envVars:
      - key: PORT
        value: 5000
