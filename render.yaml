services:
  - type: web
    name: ml-api
    env: python
    pythonVersion: 3.10.13
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python api_ml.py"
    envVars:
      - key: PORT
        value: 5000
